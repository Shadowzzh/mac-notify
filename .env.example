# 服务器配置
HOST=0.0.0.0
PORT=8079

# Master 服务地址（用于测试脚本）
MASTER_URL=http://127.0.0.1:8079

# 日志级别 (info, debug, error, warn)
LOG_LEVEL=info

# 通知声音配置
# question 类型通知的声音
NOTIFICATION_SOUND_QUESTION=Ping
# error 类型通知的声音
NOTIFICATION_SOUND_ERROR=Basso
# success/info 类型通知的声音
NOTIFICATION_SOUND_DEFAULT=default

# 通知外观配置
# 自定义通知图标（应用图标，支持相对路径、绝对路径或 URL）
# 注意：macOS 通知中心通常会强制使用发送应用的图标，此参数可能不生效
# NOTIFICATION_ICON=./assets/icons/claude.png

# 自定义通知内容图片（通知右侧显示的图片，支持相对路径、绝对路径或 URL）
# 推荐使用此参数来显示自定义图片
# macOS: 支持 .png, .icns
# Windows: 支持 .png, .ico (< 1024x1024, < 200KB)
# Linux: 支持 .png, .jpg
NOTIFICATION_CONTENT_IMAGE=./assets/icons/claude.png
# NOTIFICATION_CONTENT_IMAGE=/absolute/path/to/icon.png
# NOTIFICATION_CONTENT_IMAGE=https://example.com/icon.png

# 通知副标题（默认显示项目目录）
# NOTIFICATION_SUBTITLE=Claude Code

# 通知超时时间（秒，默认 5）
# NOTIFICATION_TIMEOUT=5

# 是否等待用户交互（true/false，默认 false）
# NOTIFICATION_WAIT=false
